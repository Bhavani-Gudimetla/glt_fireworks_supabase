// Default price-list names (the cloud database can rename them). Product data is NOT bundled here:
// it comes from the cloud after login, so cost and wholesale prices are never shipped inside a public file.
const DEFAULT_REF_MAP = {"1": "SHOP", "2": "PATTI", "3": "RETAIL BEFORE DEWALI", "4": "RETAIL DEWALI"};
const ALL_PRODUCTS = [];
